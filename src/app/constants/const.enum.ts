export enum Level{
   completed = 'completed',
   not_completed = 'not completed',
   in_progress = 'in progress'
}

export enum Difficulty{
    Very_Hard = 'Very Hard',
    Medium = 'medium',
    Easy = 'easy'
 }

 export enum Status{
    pending = 'pending',
    success = 'success',
    paused = 'paused',
    failed = 'failed'
 }
